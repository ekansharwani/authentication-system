<!doctype html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <script src="https://cdn.tailwindcss.com"></script>
        <title>Login</title>
    </head>

    <body class="bg-blue-50">
        <nav class="border-b bg-blue-700 shadow-lg">
            <div class="max-w-6xl mx-auto py-4 px-6 xl:px-0 flex items-center justify-between">
                <div class="menu-left flex items-center space-x-6">
                    <a href="/" class="text-2xl font-bold text-white">Authentication System</a>
                    <ul class="flex items-center space-x-4">
                        <li><a href="/login" class="px-4 py-2 rounded-lg text-white hover:bg-blue-800 transition">Login</a></li>
                        <li><a href="/register" class="px-4 py-2 rounded-lg text-white hover:bg-blue-800 transition">Sign Up</a></li>
                    </ul>
                </div>
            </div>
        </nav>

        <main class="flex justify-center items-center min-h-screen p-6">
            <div class="w-full max-w-lg bg-white shadow-xl rounded-xl p-8">
                <h1 class="text-3xl font-semibold text-center text-blue-700 mb-6">Log In</h1>
                <form action="/login" method="post" class="space-y-4">
                    <div>
                        <label class="block text-blue-800 font-medium">Username</label>
                        <input type="text" name="username" class="w-full mt-2 py-3 px-4 border border-blue-300 rounded-lg focus:ring-2 focus:ring-blue-600">
                    </div>
                    <div>
                        <label class="block text-blue-800 font-medium">Password</label>
                        <input type="password" name="password" class="w-full mt-2 py-3 px-4 border border-blue-300 rounded-lg focus:ring-2 focus:ring-blue-600">
                    </div>
                    
                    <% if (messages.error) { %>
                        <div class="p-4 bg-red-500 text-white rounded-lg">
                            <p><%= messages.error %></p>
                        </div>
                    <% } %>
                    
                    <div class="text-center">
                        <button type="submit" class="w-full py-3 rounded-lg text-white bg-blue-700 hover:bg-blue-800 transition font-semibold">Login</button>
                    </div>
                    <div class="border-t pt-4 text-center">
                        <p class="text-sm text-gray-700">Need an account? <a href="/register" class="text-blue-700 font-semibold">Sign Up Now</a></p>
                        <p class="text-sm text-gray-700 mt-2"><a href="/" class="text-blue-700 font-semibold">Forgot password?</a></p>
                    </div>
                </form>
            </div>
        </main>        

        <footer class="bg-blue-700 text-white py-10">
            <div class="max-w-6xl mx-auto px-6 xl:px-0 md:flex justify-between">
                <div class="footer-left mb-6 md:mb-0">
                    <h2 class="mb-4 text-lg font-semibold">About Us</h2>
                    <p>Lorem ipsum bla bla bla..</p>
                </div>
                <div class="footer-right">
                    <h2 class="mb-4 text-lg font-semibold">Social Links</h2>
                    <ul class="space-y-2">
                        <li><a href="#" class="hover:underline">Facebook</a></li>
                        <li><a href="#" class="hover:underline">Twitter</a></li>
                        <li><a href="#" class="hover:underline">Instagram</a></li>
                    </ul>
                </div>
            </div>
        </footer>

        <script src="https://unpkg.com/hyperscript@2.2.0/dist/hyperscript.js"></script>
    </body>
</html>
